# Soul backend server URL (used by the API proxy in next.config.ts)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket server URL (used for real-time chat and streaming)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
