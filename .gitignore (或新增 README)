# acp-proxy

## 项目简介

acp-proxy 是一个应用代理工具，用于处理数据传输和技能管理。它支持运行时生成状态文件、日志和配置缓存，以优化性能和管理。

## 忽略文件说明

在项目中，某些文件是应用运行时生成的，不应提交到版本控制。这些文件位于 `acp-proxy/data/` 和 `acp-proxy/skills/` 目录下，具体包括：

- `dna_*.json`：应用运行时生成的状态文件，用于存储数据和配置状态。
- `audit_log.jsonl`：应用运行时生成的审计日志文件，记录操作历史和事件。

这些文件是状态、日志和配置缓存文件，由应用在本地环境中根据运行逻辑自动生成。开发者需要确保本地环境能够正确生成这些文件，但不应将它们提交到 Git 仓库中，以避免版本控制冲突和敏感信息泄露。

请参考项目根目录下的 `.gitignore` 文件以了解更多被忽略的文件和目录。